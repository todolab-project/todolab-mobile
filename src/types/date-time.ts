/** `java.time.LocalDate` JSON representation, for example `2026-06-23`. */
export type LocalDateString = string;

/** `java.time.LocalDateTime` JSON representation without a timezone offset. */
export type LocalDateTimeString = string;
